import Navigation from "components/navigation/navigation";
import '../appBar/app-bar.css'

const AppBar = () => {
    return <header className="header">
        <Navigation/>
    </header>
}

export default AppBar